sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t roisahdad/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true roisahdad/docker-react npm run test -- --coverage